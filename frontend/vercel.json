{
  "rewrites": [
    {
      "source": "/((?!manifest.json|favicon.ico|logo[0-9]*.png|robots.txt|asset-manifest.json).+)",
      "destination": "/index.html"
    }
  ],
  "buildCommand": "npm run build",
  "outputDirectory": "build",
  "framework": "create-react-app"
}
